{
  "_comment": "The mutation gate's declaration: which files are mutation-graded, and which suites pin them. It is the only source for that pairing — the per-run Stryker config and the vitest include are both generated from it (`mutation-check.ts`), so there is no second list to keep in step. Entries arrive one at a time and deliberately: adding one asserts that the named suites, on their own, pin that file's behaviour, and the gate will say so in numbers on the next change to it. Asserted in full by `__tests__/mutation-scope.test.ts`, because a shrinking list is how this gate would pass by grading less.",
  "graded": [
    {
      "file": "scripts/audit-policy.ts",
      "suites": ["scripts/__tests__/audit-policy.test.ts"]
    },
    {
      "file": "scripts/diff-coverage-policy.ts",
      "suites": ["scripts/__tests__/diff-coverage-policy.test.ts"]
    },
    {
      "file": "scripts/mutation-policy.ts",
      "suites": ["scripts/__tests__/mutation-policy.test.ts"]
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/url.ts",
      "suites": ["apps/web/src/integrations/gitlab/server/__tests__/url.test.ts"]
    },
    {
      "file": "apps/web/src/lib/server/integrations/external-link-scope.ts",
      "suites": ["apps/web/src/lib/server/integrations/__tests__/external-link-scope.test.ts"]
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/inbound.ts",
      "suites": [
        "apps/web/src/integrations/gitlab/server/__tests__/inbound.test.ts",
        "apps/web/src/lib/server/integrations/__tests__/signature-matrix.test.ts"
      ]
    },
    {
      "file": "apps/web/src/lib/server/integrations/board-routing-policy.ts",
      "suites": ["apps/web/src/lib/server/integrations/__tests__/board-routing-policy.test.ts"]
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/post-source.ts",
      "suites": ["apps/web/src/integrations/gitlab/server/__tests__/post-source.db.test.ts"]
    },
    {
      "file": "apps/web/src/lib/server/events/resolvers/issue-move-policy.ts",
      "suites": ["apps/web/src/lib/server/events/resolvers/__tests__/issue-move-policy.test.ts"]
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "suites": ["apps/web/src/integrations/gitlab/server/__tests__/issue-move.db.test.ts"]
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/archive.ts",
      "suites": ["apps/web/src/integrations/gitlab/server/__tests__/archive.test.ts"]
    },
    {
      "file": "apps/web/src/lib/server/events/resolvers/issue-move.resolver.ts",
      "suites": [
        "apps/web/src/lib/server/events/resolvers/__tests__/issue-move.resolver.db.test.ts",
        "apps/web/src/lib/server/events/__tests__/issue-move-wiring.test.ts"
      ]
    },
    {
      "file": "apps/web/src/lib/client/mutations/inbox-list-cache.ts",
      "suites": [
        "apps/web/src/lib/client/mutations/__tests__/inbox-list-cache.test.ts",
        "apps/web/src/lib/client/mutations/__tests__/inbox-list-cache-collision.test.tsx"
      ]
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/token-renewal.ts",
      "suites": ["apps/web/src/integrations/gitlab/server/__tests__/oauth-refresh.test.ts"]
    },
    {
      "file": "apps/web/src/lib/client/failure-message.ts",
      "suites": [
        "apps/web/src/components/admin/feedback/detail/__tests__/use-metadata-handlers.test.tsx"
      ]
    },
    {
      "file": "apps/web/src/components/admin/feedback/detail/use-metadata-handlers.ts",
      "suites": [
        "apps/web/src/components/admin/feedback/detail/__tests__/use-metadata-handlers.test.tsx"
      ]
    },
    {
      "file": "apps/web/src/lib/server/domains/changelog/changelog-board-filter.ts",
      "suites": [
        "apps/web/src/lib/server/domains/changelog/__tests__/changelog-board-filter.test.ts"
      ]
    },
    {
      "file": "apps/web/src/lib/server/domains/changelog/changelog-board.service.ts",
      "suites": [
        "apps/web/src/lib/server/domains/changelog/__tests__/changelog-board.db.test.ts",
        "apps/web/src/lib/server/domains/changelog/__tests__/changelog-board-shortcut.test.ts",
        "apps/web/src/lib/server/domains/changelog/__tests__/changelog-board-write.db.test.ts"
      ]
    }
  ],
  "equivalents": [
    {
      "file": "apps/web/src/integrations/gitlab/server/inbound.ts",
      "mutator": "ConditionalExpression",
      "line": "if (!state) return null",
      "replacement": "false",
      "why": "The guard exists to narrow `state` from `string | undefined` before it indexes the status map; it decides nothing the next line does not already decide. Without it an absent or empty state produces no mapped status, and `if (!externalStatus) return null` two lines down returns null for exactly the same inputs. No payload can tell the two apart, because there is none where the first guard returns and the second would not."
    },
    {
      "file": "scripts/diff-coverage-policy.ts",
      "mutator": "EqualityOperator",
      "line": "if (highest === undefined || highest < count) known.executions.set(line, count)",
      "replacement": "highest <= count",
      "why": "The comparison keeps the highest count a shard reported. With `<=` an equal count overwrites an equal count, which stores the same number it replaced. No input can distinguish the two, so no test can."
    },
    {
      "file": "scripts/diff-coverage-policy.ts",
      "mutator": "EqualityOperator",
      "line": "if (best === undefined || best < count) executions.set(line, count)",
      "replacement": "best <= count",
      "why": "The same maximum, taken over the statements starting on one line. `<=` replaces an equal value with an equal value."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/post-source.ts",
      "mutator": "ObjectLiteral",
      "line": "const log = logger.child({ component: 'gitlab-post-source' })",
      "replacement": "{}",
      "why": "The component name is metadata on a log line and reaches no branch, no return value and no query. A test could only assert the string back from the logger it just configured, which is the tautology the mutation gate exists to expose rather than to reward."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/post-source.ts",
      "mutator": "StringLiteral",
      "line": "const log = logger.child({ component: 'gitlab-post-source' })",
      "replacement": "\"\"",
      "why": "Same line, same reason: the component name is log metadata, not behaviour. The line's operator-facing content that does matter — the post id in the warning — is asserted in post-source.db.test.ts."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/post-source.ts",
      "mutator": "ObjectLiteral",
      "line": ".select({ id: postExternalLinks.id })",
      "replacement": "{}",
      "why": "The selected row is never read: hasActiveGitLabLink returns whether one came back, not what was in it. Narrowing or widening the projection cannot change that boolean, so no input distinguishes the two."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/archive.ts",
      "mutator": "ConditionalExpression",
      "line": "if (ctx.externalUrl) {",
      "replacement": "true",
      "why": "The guard only spares a throw the catch beside it absorbs: new URL rejects null, undefined and the empty string alike, and both paths end at the same gitlab.com default. Measured on all three falsy values."
    },
    {
      "file": "apps/web/src/lib/server/events/resolvers/issue-move.resolver.ts",
      "mutator": "ConditionalExpression",
      "line": "const rule = rulesFromMappings(rows).find((r) => r.boardId === boardId)",
      "replacement": "true",
      "why": "Two constraints upstream already hold what the predicate checks: the query selects only mappings whose targetKey is this board, and completeRuleOf returns no rule unless filters.boardIds[0] equals that same targetKey. Every rule that reaches find therefore has boardId === boardId, so no stored row makes the predicate decide anything."
    },
    {
      "file": "apps/web/src/lib/server/events/resolvers/issue-move.resolver.ts",
      "mutator": "ConditionalExpression",
      "line": "if (links.length === 0) return []",
      "replacement": "false",
      "why": "Only the number of queries changes. With no links, buildIssueMoveTargets returns the same empty list the short circuit returns, so the guard saves a lookup and decides nothing."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "ObjectLiteral",
      "line": "const log = logger.child({ component: 'gitlab-issue-move' })",
      "replacement": "{}",
      "why": "The component name is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "StringLiteral",
      "line": "const log = logger.child({ component: 'gitlab-issue-move' })",
      "replacement": "\"\"",
      "why": "The component name is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "ObjectLiteral",
      "line": "log.error({ err: error, link_id: linkId, to_project_id: toProjectId }, 'issue move threw')",
      "replacement": "{}",
      "why": "The field set is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "StringLiteral",
      "line": "log.error({ err: error, link_id: linkId, to_project_id: toProjectId }, 'issue move threw')",
      "replacement": "\"\"",
      "why": "The message is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "ObjectLiteral",
      "line": "log.error({ link_id: linkId, to_project_id: toProjectId }, 'move answered without an iid')",
      "replacement": "{}",
      "why": "The field set is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "StringLiteral",
      "line": "log.error({ link_id: linkId, to_project_id: toProjectId }, 'move answered without an iid')",
      "replacement": "\"\"",
      "why": "The message is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart. The refusal an operator acts on is the result's own error text, which a test does assert."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "ObjectLiteral",
      "line": "{ link_id: linkId, from_project_id: fromProjectId, to_project_id: toProjectId },",
      "replacement": "{}",
      "why": "The field set is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "StringLiteral",
      "line": "'issue moved'",
      "replacement": "\"\"",
      "why": "The message is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "ObjectLiteral",
      "line": "{ status_code: status, link_id: linkId, to_project_id: toProjectId, body },",
      "replacement": "{}",
      "why": "The field set is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/integrations/gitlab/server/issue-move.ts",
      "mutator": "StringLiteral",
      "line": "'move refused'",
      "replacement": "\"\"",
      "why": "The message is metadata on a log line. It reaches no branch, no return value and no query, and nothing in the result an operator or the worker reads is built from it, so no input tells the two apart."
    },
    {
      "file": "apps/web/src/lib/server/domains/changelog/changelog-board.service.ts",
      "mutator": "ConditionalExpression",
      "line": "if (filter.boardIds.length === 0) return sql`NOT ${assigned}`",
      "replacement": "false",
      "why": "The shortcut and the general form build predicates that mean the same thing. Falling through yields `NOT assigned OR EXISTS (SELECT 1 ... AND <empty inArray>)`, and drizzle renders an empty `inArray` as the literal `false` (measured on 0.45.2), so the EXISTS is unsatisfiable and the OR collapses back to `NOT assigned` for every row. No entry, product or reader can be arranged where the two disagree, because they are the same predicate written twice. The branch is kept rather than deleted because the general form only reduces correctly through that empty-inArray rendering, which is drizzle's choice and not ours to rely on."
    }
  ]
}
